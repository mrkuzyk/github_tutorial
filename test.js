// Change code below this line
const getUsersWithEyeColor = (users, color) => {
 const colorEye = users.filter(user => user.eyeColor === color);
 return colorEye

};
// Change code above this line

// функція повертає користувачів, чий колірОчей === кольору