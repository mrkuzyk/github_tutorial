// Change code below this line
const getUserEmails = users => {
    const email = users.map(user => user.email);
    return email

  };
  // Change code above this line


//   функція повертає масив поштових адрес користувачів