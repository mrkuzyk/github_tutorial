function includes(array, value) {
  // Change code below this line
for (const arra of array)
if (arra === value){
return true
}

return false;
  // Change code above this line
}

// функція шукає спільне значення в "арай" та "валуе"
// без використання includes